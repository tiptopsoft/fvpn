version: "2.2"

services:

  registry:
    image: star:0.1
    ports:
      - "3001:3000/udp"
    command:
      - "client"
    volumes:
      - /Users/francis/workspc/star/conf/app.yaml:/etc/star/app.yaml

  edge:
    image: star:0.1
    ports:
      - "4000:4000/udp"
      - "4009:4009"
    command:
      - "server"
    volumes:
      - /Users/francis/workspc/star/conf/app.yaml:/etc/star/app.yaml